using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinearInterpolation : MonoBehaviour
{ /*
    // Start is called before the first frame update
    void Start()
    {
   // the Mathf.Lerp function takes 3 float variables that all represent different values. Interpolate form, interpolate to and how far to interpolate.
    float result = Mathf.Lerp (3f, 5f, 0.5f); 
   
    // Vector3 is the same as mathf.lerp but the from and to values are type vector3.
    Vector3 from = new Vector3 (1f, 2f, 3f);
    Vector3 to = new Vector3 (5f, 6f, 7f);
    Vector3 result = Vector3.Lerp (from, to, 0.75f);
    }

    // Update is called once per frame
    void Update()
    {
    // light intensity changed per frame
    light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f);
   
    // light intesity changes per second
    light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f * Time.deltaTime);
    }
    */
}
